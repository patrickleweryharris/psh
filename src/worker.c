#include <stdio.h>
#include <stdlib.h>
#include <string.h>

// Calls for dependencies
#include "pshell.h"

void main_shell(){

  char *a = malloc(sizeof(char) * MAXLINE);
  while (1){
    scanf("%s", a);

  }

}
